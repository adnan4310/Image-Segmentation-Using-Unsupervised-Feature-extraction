# Image-Segmentation-Using-Unsupervised-Feature-extraction
An unsupervised image segmentation approach using multi-feature extraction (color, edge, texture) and K-Means clustering. The system refines segmentation with morphological operations and enables object extraction without labeled data.
